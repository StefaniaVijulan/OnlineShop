import { NgSearchPipePipe } from './ng-search-pipe.pipe';

describe('NgSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NgSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
